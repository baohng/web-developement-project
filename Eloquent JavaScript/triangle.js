(function triangle() {
  const triangleWidth = 7;
  let signal = "";
  for (let item = 1; item <= triangleWidth; item++) {
    console.log(signal+=`#`);
  }
})();
console.log(`-----------------------`);